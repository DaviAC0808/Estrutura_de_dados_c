#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

typedef struct no
{
    int Vlr;
    struct no *prox;
} no;

typedef struct fila
{
    struct fila *inicio;
    struct fila *fim;
}fila;

void criar() 

int main()
{
    setlocale(LC_ALL, "Portuguese_Brazil");
    int x;

    printf("\n\n");
    // scanf("%", );

}