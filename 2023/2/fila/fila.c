#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

typedef struct no
{
    int Vlr;
    struct no *prox;
} no;

typedef struct fila
{
    struct fila *inicio;
    struct fila *fim;
}fila;

void criar(fila *f){
    f->inicio = NULL
    f->fim
} 

int main()
{
    setlocale(LC_ALL, "Portuguese_Brazil");
    int ;

    printf("\n\n");
    // scanf("%", );

}