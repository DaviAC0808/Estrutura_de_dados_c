#include <stdio.h>
#include <stdlib.h>

int main()
{

    int verdadeiro, falso, var;
   
    verdadeiro = (15 > 20);
    falso = (15 == 20);

    printf("Digite um numero: %d", &var);
    scanf(var);

    if (var>4 && !(var<9)||var<=4)
    {
        sizeof(var);
    }
    
}
    